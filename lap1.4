//จงเขียนคำสั่งในการสร้างและเรียกใช้งานฟังก์ชัน GetSet() เพื่อรับจำนวนสมาชิกและค่าของแต่ละสมาชิกในเซต จากคีย์บอร์ด ตามโปรโตไทป์ต่อไปนี้ 
#include <stdio.h>
#include <stdlib.h> 
// Adjusted prototype to accept a pointer to a pointer to int
int GetSet(int **data);

int main() {
    int *data = NULL;
    int num;


    num = GetSet(&data);


    for (int i = 0; i < num; ++i) {
        printf("Element %d: %d\n", i, data[i]);
    }


    free(data);

    return 0;
}

int GetSet(int **data) {
    int num;
    printf("Enter the number of elements: ");
    scanf("%d", &num);

    *data = (int *)malloc(num * sizeof(int));
    if (*data == NULL) {
        // Memory allocation failed, return 0 elements
        printf("Memory allocation failed\n");
        return 0;
    }


    for (int i = 0; i < num; ++i) {
        printf("Enter element %d: ", i);
        scanf("%d", (*data) + i);
    }

    return num;
}
